---
title: '5조 기말과제 제안서'
subtitle: "팝업스토어 프로세스 최적화 및 개선을 위한 운영 시스템 개발"
categories: ["보고서", "공급사슬관리"]
mainfont: "Noto Sans KR"
sansfont: "Noto Sans KR"
monofont: "Noto Sans KR"
date: last-modified
geometry:
  - top=15mm
  - left=15mm
  - right=15mm
  - bottom=15mm
format:
  titlepage-pdf:
    documentclass: scrartcl
    classoption: ["oneside", "open=any", "twocolumn"]
    number-sections: true
    toc: false
    titlepage: "none"
    coverpage: "none"
    fontsize: 9pt
    include-in-header:
      text: |
        \usepackage{cuted}
        \usepackage[utf8]{inputenc}
        \usepackage{graphicx}
        \usepackage{amsmath}
        \usepackage{amsfonts}
        \usepackage{amssymb}
        \usepackage{geometry}
        \usepackage{fancyhdr}
        \usepackage{hyperref}
        \usepackage{lipsum}
        \setlength{\columnsep}{10mm}
        \usepackage{setspace}
        \setstretch{1.3}
        \usepackage{booktabs}
        \usepackage{tabularx}
        \usepackage{makecell}
        \usepackage{caption}
        \usepackage{multirow}
        \usepackage{array}
        \usepackage{graphicx}
        \renewcommand{\arraystretch}{1.3}
        \renewcommand{\tabularxcolumn}[1]{m{#1}}
        \raggedbottom

execute:
  echo: false
  warning: false
  message: false
---

\begin{strip}
\vspace{-3em}
\begin{center}
\makeatletter
  \thispagestyle{empty}

  \noindent 프로세스경영(나) 주제 제안서 \hfill \texttt{Last Updated: \@date} \\
  \noindent\rule{\textwidth}{1pt}\par

  \vspace{2em}

  {\large \textbf{\@title}} %
  \vspace{1em}
  \\
  {\huge \textbf{\@subtitle}} \\

  \vspace{1em}
  \noindent\rule{\textwidth}{1pt}\par

  \vspace{1em}

  \textbf{김형훈$^1$, 강지민$^2$, 우종민$^3$, 민종호$^4$}

  \vspace{0.5em}

  $^1$숭실대학교 산업정보시스템공학과 20192208 \\
  $^2$숭실대학교 산업정보시스템공학과 20201309 \\
  $^3$숭실대학교 산업정보시스템공학과 20192231 \\
  $^4$숭실대학교 산업정보시스템공학과 20211281

  \vspace{0.5em}

  \noindent\rule{\textwidth}{0.3pt}\par
\makeatletter
\end{center}
\end{strip}

# 배경 및 문제 정의

최근 팝업스토어는 브랜드 경험 강화를 위한 핵심 채널로 빠르게 확대되고 있다. 이에 따라 운영의 표준화와 효율화 필요성도 함께 커지고 있으며, 특히 대기열(줄) 관리의 비효율은 체류 시간 증가와 고객 이탈률 상승으로 직결되어 매출과 브랜드 경험 모두에 부정적 영향을 미친다. 또한 팝업의 특성상 프로젝트 수명이 짧고 변동성이 커서, 기획부터 철수까지 전 과정에서 표준 운영절차(SOP)와 데이터 기반 의사결정 체계가 부재할 경우 품질 편차, 재작업, 비용 증가 위험이 확대된다.

현장 운영은 임시 인력 비중이 높아 교육 리드타임과 휴먼에러가 빈발하고, 온라인(예약·홍보·데이터 수집)과 오프라인(입장·동선·결제) 간 데이터가 단절되는 문제가 존재한다. 그 결과 재고 가용성 파악, 수요 대응, 회계 정산의 정합성 확보가 어려워진다. 본 프로젝트는 프로세스경영 관점에서 이러한 이슈를 구조적으로 정의한다.

# 목표

본 과제는 기획–오픈–운영–철수의 전 과정을 대상으로 리드타임을 단축하고 대기 경험을 개선하며, 인력 및 외주비를 최적화하여 매출과 전환율을 향상시키는 것을 목표로 한다. 공간 대여 플랫폼 구축이 아니라, 정기적으로 팝업을 운영하는 기업의 내부 운영 프로세스에 초점을 두고 표준화·자동화·시각화를 추진한다. 이에 따라 As-Is/To-Be 프로세스 모델 수립, 병목과 변동성의 원인 분석, 대기열·재고 보충 등 핵심 서브프로세스의 간이 시뮬레이션 및 용량 산정, 체크리스트·표준작업서(SOP) 초안과 운영 지표(KPI) 정의, 간단한 운영 대시보드와 입장관리 흐름의 프로토타입 제시를 주요 산출물로 설정한다.

# 범위 및 수행 방법

범위는 사전 수요예측과 물량계획, 입지 및 동선 설계, 대기열·입장 관리, 재고 보충과 진열, 결제 및 회계 정산, 피크 운영, 마감과 철수에 이르는 전 단계를 포함한다. 적용 방법으로는 공개 자료와 선행연구 검토, 사례 비교, 가능 시 제한적 현장 관찰·인터뷰를 활용한다. 평가는 간이 시뮬레이션과 정량·정성 지표를 결합해 수행한다. 제약과 가정으로는 단기간 수행을 전제로 하며, 시스템 구현은 기능 시나리오와 프로토타입 수준으로 한정하고, 상세 IT 개발과 전사적 재무·법무 검토는 제외한다.

\clearpage

# 추진 일정 및 역할 분담

```{r}
#| fig-width: 12
#| fig-height: 8
#| out-width: "100%"
library(ggplot2)
library(dplyr)
library(lubridate)
library(showtext)

# Set up fonts
font_add_google("Noto Sans KR", "Noto Sans KR")
showtext_auto()

# Generate date data
start_date <- as.Date("2025-11-01")
end_date <- as.Date("2025-11-30")
dates <- seq(start_date, end_date, by = "day")
weeks <- ceiling((day(dates) + wday(start_date, week_start = 7) - 1) / 7)

# Define updated task descriptions for each week
task_grouped_labels <- c("1. 주제 선정", "2. 자료 수집", "3-4. 모델링 및 검토", "5. 개선 및 발표 자료 검토")

calendar_data <- data.frame(
  date = dates,
  day = wday(dates, label = TRUE, abbr = TRUE, week_start = 7),
  week = weeks,
  task = factor(c(1, 2, 3, 3, 4, NA)[weeks],
              levels = 1:4,
              labels = task_grouped_labels),
  is_wednesday = wday(dates) == 1
)

# 회의 정보와 최종 산출물 정보 추가
calendar_data$meeting_text <- ifelse(calendar_data$is_wednesday,
                                   "14:00-16:00\n비대면 회의 진행",
                                   "")

# 최종 산출물 정보 추가
calendar_data$output_text <- ifelse(
  day(calendar_data$date) == 1, "모두: 주제 제안",
    ifelse(day(calendar_data$date) == 8, "\n강지민: 현장조사 대상선정\n김형훈, 민종호: 운영자료 조사\n우종민: 기획자료 조사",
            ifelse(day(calendar_data$date) == 15, "민종호: As-Is 모델링\n모두: 현장 조사",
                ifelse(day(calendar_data$date) == 22, "민종호: To-Be 모델링\n김형훈, 우종민: 자동화",
                    ifelse(day(calendar_data$date) == 29, "강지민: 상세 PPT\n제작 및 발표",
                        ifelse(day(calendar_data$date) == 30, "\n\n\n발표 자료 제출",
                            ifelse(day(calendar_data$date) == 9, "\n\n\n제안서 제출",
                                ifelse(day(calendar_data$date) == 2, "\n\n\n주제 선정 완료",
                                   ""))))))))

# Colors for each week
week_colors <- c("#98FB98", "#87CEEB", "#DDA0DD", "#FFD700", "#FFA07A")

# Define weekday labels
weekdays <- c("Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat")

# Plot with ggplot2
ggplot(calendar_data, aes(x = day, y = -week)) +
  geom_tile(aes(fill = task), color = "white", size = 0.8) +
  # 날짜 표시 (수요일은 빨간색으로)
  geom_text(aes(label = day(date),
                color = ifelse(is_wednesday, "red", "black")),
            size = 3,
            hjust = 0,
            vjust = 1,
            position = position_nudge(x = -0.35, y = 0.35)) +
  # 회의 정보 추가
  geom_text(aes(label = meeting_text),
            color = "black",
            size = 3,
            hjust = 0.5,
            vjust = 0.5) +
  # 최종 산출물 정보 추가
  geom_text(aes(label = output_text),
            color = "black",
            size = 3,
            hjust = 0.5,
            vjust = 0.5) +
  scale_fill_manual(values = week_colors[1:4],
                   name = "",
                   labels = task_grouped_labels,
                   na.translate = FALSE,
                   na.value = "white") +
  scale_color_identity() +
  theme_minimal(base_family = "Noto Sans KR") +
  theme(
    axis.text.y = element_blank(),
    axis.text.x = element_blank(),
    axis.title = element_blank(),
    panel.grid = element_blank(),
    legend.text = element_text(size = 10),
    legend.title = element_text(size = 1),
    legend.position = "bottom",
    aspect.ratio = 0.7
  ) +
  guides(fill = guide_legend(nrow = 1)) +
  annotate("text", x = 1:7, y = 0, label = weekdays, size = 4,
           family = "Noto Sans KR", fontface = "bold", vjust = 1)
```

\vspace{0.5em}

- 비대면 회의에서는 이전 주의 작업 내용을 검토하고, 해당 주의 세부 작업을 계획한다.
